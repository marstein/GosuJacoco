#!/bin/bash

for i in $( /bin/ls -1 ~/jacoco/bin/*.jar )
do
    CP=$CP:$i
done

# -b branch -c changelist -a apps cc pc -f file -runDate -jdbc
java -cp ".:$CP" gw.jacoco.sourcereport.SourceReport $*
